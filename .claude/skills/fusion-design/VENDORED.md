# Vendored copy

`fusion-design/` is a copy of the skill from the FusionHelper project:

    C:\Users\gethi\source\FusionHelper\skills\fusion-design

Copied 2026-09-06. FusionHelper is the source of truth. Re-copy after any
change there rather than editing this copy.

The sibling skill `print-in-place-design` (same source directory) is referenced
by `reference/hazards.md` but is not installed here.

The `fusionhelper` Python package (preflight gate, bundler, verify block) is a
separate dependency and is already importable from the FusionHelper working copy.
